name: Build OpenWrt Package

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      OPENWRT_VERSION: "24.10.4"
      TARGET: "mediatek"
      SUBTARGET: "filogic"
      ARCH: "aarch64_cortex-a53"
      GO_VER: "1.25.5"
          
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
            python3-setuptools rsync swig unzip zlib1g-dev file wget zstd

      - name: Install Go ${{ env.GO_VER }}
        run: |
          wget -q https://go.dev/dl/go${{ env.GO_VER }}.linux-amd64.tar.gz
          sudo tar -C /usr/local -xzf go${{ env.GO_VER }}.linux-amd64.tar.gz
          echo "/usr/local/go/bin" >> $GITHUB_PATH

      - name: Download OpenWrt SDK
        run: |
          SDK_URL="https://downloads.openwrt.org/releases/24.10.4/targets/mediatek/filogic/openwrt-sdk-24.10.4-mediatek-filogic_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          wget -q "$SDK_URL" -O sdk.tar.zst
          tar --zstd -xf sdk.tar.zst
          mv openwrt-sdk-24.10.4-mediatek-filogic* openwrt-sdk

      - name: Prepare SDK environment
        working-directory: openwrt-sdk
        run: |
          mkdir -p staging_dir/hostpkg/bin
          mkdir -p staging_dir/host/bin
          ln -sf /usr/local/go/bin/go staging_dir/hostpkg/bin/go
          ln -sf /usr/local/go/bin/gofmt staging_dir/hostpkg/bin/gofmt
          ln -sf /usr/local/go/bin/go staging_dir/host/bin/go
          touch staging_dir/hostpkg/.built_golang_host

      - name: Update and install feeds
        working-directory: openwrt-sdk
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a golang
          ./scripts/feeds uninstall dnscrypt-proxy

      - name: Link Local Source to SDK
        run: |
          # Копируем ВЕСЬ твой текущий репозиторий (включая go.mod и папку dnscrypt-proxy)
          mkdir -p openwrt-sdk/package/dnscrypt-proxy
          rsync -av . openwrt-sdk/package/dnscrypt-proxy/ --exclude openwrt-sdk --exclude .git

      - name: Configure
        working-directory: openwrt-sdk
        run: |
          echo "CONFIG_PACKAGE_dnscrypt-proxy=m" > .config
          echo "CONFIG_USE_EXTERNAL_GO=y" >> .config
          echo "CONFIG_EXTERNAL_GO_DIRECTORY=\"/usr/local/go\"" >> .config
          make defconfig

      - name: Compile package
        working-directory: openwrt-sdk
        run: |
          # Используем внутренний линкер, arm64 и внешний Go
          make package/dnscrypt-proxy/compile V=s -j$(nproc) \
            GO_VERSION="${{ env.GO_VER }}" \
            GO_BIN="/usr/local/go/bin/go" \
            GOROOT="/usr/local/go" \
            GO_ARCH="arm64"

      - name: Debug find
        run: find openwrt-sdk/bin/packages -name "dnscrypt-proxy*.ipk"

      - name: Upload packages
        uses: actions/upload-artifact@v4
        with:
          name: dnscrypt-proxy-24.10.4-${{ env.ARCH }}
          # Используем двойную звездочку для поиска во всех подпапках
          path: openwrt-sbin/bin/packages/${{ env.ARCH }}/**/dnscrypt-proxy*.ipk
          if-no-files-found: error

name: Build OpenWrt Package

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      OPENWRT_VERSION: "24.10.4"
      TARGET: "mediatek"
      SUBTARGET: "filogic"
      ARCH: "aarch64_cortex-a53"
      
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
            python3-setuptools rsync swig unzip zlib1g-dev file wget zstd

      - name: Download OpenWrt SDK
        run: |
          SDK_URL="https://downloads.openwrt.org/releases/${OPENWRT_VERSION}/targets/${TARGET}/${SUBTARGET}/openwrt-sdk-${OPENWRT_VERSION}-${TARGET}-${SUBTARGET}_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          wget -q $SDK_URL -O sdk.tar.zst
          tar --zstd -xf sdk.tar.zst
          mv openwrt-sdk-* openwrt-sdk

      - name: Update and install feeds
        working-directory: openwrt-sdk
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a golang
          ./scripts/feeds uninstall dnscrypt-proxy

      - name: Link package
        run: |
          # Привязываем ваш текущий репозиторий как пакет в SDK
          ln -s $GITHUB_WORKSPACE openwrt-sdk/package/dnscrypt-proxy

      - name: Install Go 1.24+ manually
        run: |
          wget -q https://go.dev/dl/go1.25.5.linux-amd64.tar.gz
          sudo tar -C /usr/local -xzf go1.25.5.linux-amd64.tar.gz
          echo "/usr/local/go/bin" >> $GITHUB_PATH
          echo "GOROOT=/usr/local/go" >> $GITHUB_ENV

      - name: Override SDK Go
        working-directory: openwrt-sdk
        run: |
          # Создаем структуру директорий заранее
          mkdir -p staging_dir/hostpkg/bin
          mkdir -p staging_dir/host/bin
          # Линкуем Go везде, где SDK может его искать
          ln -sf /usr/local/go/bin/go staging_dir/hostpkg/bin/go
          ln -sf /usr/local/go/bin/gofmt staging_dir/hostpkg/bin/gofmt
          ln -sf /usr/local/go/bin/go staging_dir/host/bin/go

      - name: Configure
        working-directory: openwrt-sdk
        run: |
          echo "CONFIG_PACKAGE_dnscrypt-proxy=m" > .config
          # Добавляем принудительное использование внешнего Go в конфиг
          echo "CONFIG_USE_EXTERNAL_GO=y" >> .config
          echo "CONFIG_EXTERNAL_GO_DIRECTORY=\"/usr/local/go\"" >> .config
          make defconfig

      - name: Compile package
        working-directory: openwrt-sdk
        run: |
          # Компилируем, явно указывая пути к внешнему Go
          make package/dnscrypt-proxy/compile V=s -j$(nproc) \
            HOST_GO_BIN=/usr/local/go/bin/go \
            GO_BIN=/usr/local/go/bin/go

      - name: Find all IPK files
        working-directory: openwrt-sdk
        run: find bin/packages -name "*.ipk" -type f
          
      - name: Upload packages
        uses: actions/upload-artifact@v4 # Рекомендую v4, v6 еще официально не вышла как стабильная
        with:
          name: dnscrypt-proxy-${{ env.OPENWRT_VERSION }}-${{ env.ARCH }}
          path: openwrt-sdk/bin/packages/${{ env.ARCH }}/packages/dnscrypt-proxy*.ipk
          if-no-files-found: error

      - name: Create release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: openwrt-sdk/bin/packages/${{ env.ARCH }}/packages/dnscrypt-proxy*.ipk
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
